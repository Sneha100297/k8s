apiVersion: v1
kind: Service 
    name: vertex-svc
    labels: 
      app: mynginx
spec: 
    type: NodePort
    ports: 
      -port: 80
       nodePort: 3001
       protocol: TCP
    selector:
       app: mynginx
          
